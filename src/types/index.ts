export enum FeedbackCategory {
  Feature = "feature",
  UI = "ui",
  UX = "ux",
  Enhancement = "enhancement",
  Bug = "bug",
}

export enum FeedbackStatus {
  Planned = "planned",
  InProgress = "in-progress",
  Live = "live",
}
